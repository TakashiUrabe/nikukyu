<div class="max-w-md pb-20">
    <h1 class="text-2xl text-gray-700 my-2 text-center">マイページ</h1>
    <p>※こちらには最後にダウンロードされたプロフィールカードが
    1匹につき1枚表示されます</p>
    <%= turbo_frame_tag "image-list" do %>
      <div>
        <% @profile_cards.each do |profile_card| %>
          <div class='py-5'>
            <%= image_tag profile_card.profile_card_data_a.to_s if profile_card.card_type == "A" %>
            <%= image_tag profile_card.profile_card_data_b.to_s if profile_card.card_type == "B" %>
            <%= image_tag profile_card.profile_card_data_c.to_s if profile_card.card_type == "C" %>
            <%= image_tag profile_card.profile_card_data_d.to_s if profile_card.card_type == "D" %>
          </div>
          <div class="flex my-3 justify-center">
            <%= link_to 'このカードを編集する', profile_card_path(profile_card), class:'btn btn-primary mx-2' %>
            <%= link_to '他のカードをダウンロードする', profile_card_path(profile_card, anchor: "another_download"), class:'btn btn-primary mx-2' %>
            <div  class='btn btn-primary mx-2'>
            <%= link_to '診断結果を見る', controller: :profile_cards, action: :result, id: profile_card.id %>
            </div>
          </div>
        <% end %>
      </div>
      <%= paginate(@profile_cards) %>
    <% end %>
  </div>
</div>
